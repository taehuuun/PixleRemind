namespace LTH.ColorMatch.Enums
{
    public enum ChangeColorType { Red, Green, Blue }
    public enum Difficulty {Easy, Normal, Hard}
    public enum GalleryTopic {Animal}
    public enum GalleryPage { Topic, PixelArt, ColorMatch }
}
